namespace SecureConnectionString.Cryptos
{
    public enum HashAlgorithmType { MD5, SHA1, SHA256, SHA384, SHA512 }
    public enum SymmetricAlgorithmType { DES, TripleDES, AES, Rijndael, RC2 }
    public enum EncryptedStringFormat { Base64, Hex }
}
